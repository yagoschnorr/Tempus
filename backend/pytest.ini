[pytest]
pythonpath = .
testpaths = tests
markers =
    integration_real: testes que sobem Postgres+pgvector via testcontainers e tocam a OpenAI (replay via VCR por padrão; opt-in --record-mode=once para gravar)
